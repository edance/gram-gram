<h1>My Photos</h1>

<% if @photos.empty? %>
  <p>You have no posts</p>
<% end %>

<% @photos.each do |p| %>
  <%= link_to(photo_path(p)) do %>
    <%= image_tag(p.ig_permalink) %>
  <% end %>
  <br/>
<% end %>
